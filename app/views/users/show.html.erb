<h1>This is a show page</h1>

<%= @user.name %>

<% @user.meds.each do |med| %>
  <p>
    <%= med.med_name %>
  </p>
  <% med.alerts.each do |alert| %>
    <p> <%= alert.alert_time.strftime("Your alert for #{med.med_name} is scheduled for %I:%M%p") %> </p>
  <% end %>
<% end %>
